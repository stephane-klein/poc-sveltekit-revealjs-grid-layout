# POC to setup Reveal.js in SvelteKit projet + Grid Slide layout based on CSS grid

Context issue in French: https://github.com/stephane-klein/backlog/issues/369

Getting started:

```sh
$ rtx install
$ pnpm install
$ pnpn run dev
```
